version: '3.8'

services:
  proxy:
    image: nginx:alpine
    container_name: absolute_proxy
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
